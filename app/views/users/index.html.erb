<h1>Admin Section</h1>
<p>Students can't get here, we have multple levels of access</p>

<h5>Log</h5>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %> signed up <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>
